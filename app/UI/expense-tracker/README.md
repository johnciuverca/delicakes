# Expense Tracker

This UI is served by the main app server.

## Development Setup

Use the repo-level scripts documented in the top-level README:

- Start: `./scripts/start-servers.sh --watch`
- Stop: `./scripts/stop-servers.sh`
