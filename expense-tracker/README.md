
# Start Expense Tracker
This is a simple expense tracker application that allows you to keep track of your expenses.

# Development Setup
To run the application locally, follow these steps:

1. run `npm install` to install all dependencies.
2. run `npm run build` to start the development server.
3. start the app server with `npm start` in '../app/'.
4. start data api server with `npm start` in '../api/'.
5. Open your browser and navigate to `http://localhost:3000` to access the application.

To have a continuous development setup, you can execute `npm run watch` to watch for changes and rebuild the project automatically