import React from "react";

const NewComponent = () => {
      return (

            <div>
                  <h1>Current Time is: {Date.now()}</h1>
                  <p>This is a newly created component in the expense tracker application.</p>
            </div>
      );
};

export default NewComponent;